services:
  redis:
    image: redis:latest

  hello-world:
    image: nginx:latest
    restart: always

x-uffizzi:
  continuous_preview:
    delete_preview_after: 10h

x-uffizzi-ingress:
  service: nginx
  port: 8080
